migti-noida-deploy:
  stage: deploy
  tags:
    - migti-prod
    - linux
  rules:
    - if: $CI_COMMIT_BRANCH == "migti_noida"
  variables:
    GIT_STRATEGY: none
    DEPLOY_PATH: /migti/noida/migti_frontend
    DEPLOY_BRANCH: migti_noida
  script:
    - cd "${DEPLOY_PATH}" && bash ci/deploy-frontend.sh
