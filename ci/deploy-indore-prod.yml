prod-indore-deploy:
  stage: deploy
  tags:
    - migti-prod
    - linux
  rules:
    - if: $CI_COMMIT_BRANCH == "prod"
  variables:
    GIT_STRATEGY: none
    DEPLOY_PATH: /migti/migti_frontend
    DEPLOY_BRANCH: prod
  script:
    - cd "${DEPLOY_PATH}" && bash ci/deploy-frontend.sh
